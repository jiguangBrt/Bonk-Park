# Development Plan — Bonk Park

Rolling commitment log. One line per Session (= one class/day) in the format:

> By Session X, I will <do thing>. Evidence: <link to PR / commit / Issue / doc>.

Past work is summarised once at the top so the timeline reads complete.

## Past work (Sessions 1 – last)

The project started as a 3D template and was migrated to 2D before any gameplay landed (PRs #4, #7). The game concept document landed next (#14), followed by the park scene with mouse-driven Lumi and a letterbox camera (#16). The Bat enemy was added with a reaction-buffer chase (#20, #22), then the bonk-reaction rock obstacle (#28). Two fixes followed: a letterbox guard and a bat-unstick patch (#26, #29). A lifecycle refactor cleaned up the scripts (#31). The first two class playtests are logged in #23.

## Upcoming commitments

[ ] **By Session <TODO>, I will <TODO>. Evidence: <TODO>.**
